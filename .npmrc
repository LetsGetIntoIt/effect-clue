# By default, use exact package versions
# Renovate Bot will pin dependencies to exact versions anyway,
# so this is just a convenience to do that automatically when developing locally.
save-prefix=''
